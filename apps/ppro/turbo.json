{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/", "tsconfig.json", "rollup.config.js"],
      "outputs": ["dist/"]
    }
  }
}
